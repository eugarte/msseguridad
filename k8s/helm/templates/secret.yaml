apiVersion: v1
kind: Secret
metadata:
  name: {{ include "msseguridad.fullname" . }}-secrets
  labels:
    {{- include "msseguridad.labels" . | nindent 4 }}
type: Opaque
stringData:
  # These should be overridden by external secret management (e.g., External Secrets Operator)
  # Placeholder values for development only
  DB_PASSWORD: "change-me-in-production"
  REDIS_PASSWORD: "change-me-in-production"
